# Server
PORT=3000

# PostgreSQL
# Use DB_HOST=db when running via Docker Compose
# Use DB_HOST=localhost when running locally outside Docker
DB_HOST=db
DB_PORT=5432
DB_NAME=proyecto_paginas_web
DB_USER=postgres
DB_PASSWORD=your_db_password
